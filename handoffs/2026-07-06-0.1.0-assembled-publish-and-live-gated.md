# Handoff — 0.1.0 fully assembled on `p3-instrumentation`; two gates left, both yours

**Date:** 2026-07-06 · **From:** orchestrator (curator seat), autonomous run · **Branch:**
`p3-instrumentation` (pushed to origin) · **`main`:** untouched, still `0.0.0`.
Cold-start: `/orient`, then read this file. The record is authoritative.

## State in one line

**0.1.0 is code-complete, tested, committed, and pushed to `origin/p3-instrumentation`.**
Two things stand between here and a public release, and **both are yours** — I stopped at
them deliberately, not because I ran out of road:
1. **The publish** (`p3 → main` merge) — irreversible, public, and you wanted it gated on
   the live falsification below.
2. **The live falsification** — blocked on the host GPU (no room; see below). It is the
   agreed gate before publish, and it has **not run**.

## What's in 0.1.0 (all committed on `p3`)

| SHA | what | issue |
|---|---|---|
| `dfb0769` | loader: quant selector → `["none"]` only (bnb can't touch the fused MoE experts), `local_files_only` toggle (default off = keep HF download), clean load errors; `bitsandbytes` dep dropped | #18 |
| `1916081` | sampler: 4th output `frames` — per-step raw canvas decode as a `STRING` list (`OUTPUT_IS_LIST`), the in-graph flipbook via `dgemma.loop.decode_frames` | — |
| `9470082` | `examples/p3-trace-annotated.ui.json` — the current graph in canvas form, 4 grounded Note nodes; provenance in `examples/README.md` | — |
| `8ac3c2f` | `loose-ends.md`: DiffusionGemma ≤ AR-base throughput is structural (KV-cache asymmetry) — **sell instrumentability, not speed** | — |
| _(this pass)_ | `live` pytest marker + `conftest.py` gate + `tests/test_live_seams.py` (3 seam tests) + `tests/README.md`; RTX-3090 evidence png; this handoff | — |

**Conceptualization decision banked (do not re-litigate):** the loader stays on transformers
HF `from_pretrained` (auto-download + cache). The `folder_paths` dropdown from #17 was
**rejected** for the sharded path (would force users to hand-place ~20 shards) and **deferred**
to #15's single-file (GGUF) graduation. See issue #17's comment.

## Gate 1 — the live falsification (BLOCKED, resumable)

**Why it exists:** the mocked unit suite has 100% line+branch coverage but buys it with fakes
at the two real seams — `model.py`'s load is mocked `from_pretrained`, and `decode_frames`
(now on the **always-run** sampler path) is only tested against a `_FakeProcessor` decoding
`torch.tensor([1,2])`, never a real tokenizer on a real per-step canvas. **If `decode_frames`
throws on real canvas data, every generation breaks** — for users who never touch the frames
output. Coverage cannot reach this; only a real run can.

**The instrument is built:** `pytest -m live` collects 4 tests (`test_load_model_real`,
`test_run_and_decode_frames_real`, `test_sampler_node_frames_real`, + the integration smoke),
each skipping gracefully if weights/CUDA absent. **They have not been executed.**

**Why blocked:** the host RTX-8000 has **~8 GB free of 48 GB** — the resident chat server
(`:8081`, ~37 GB) + embeddinggemma (`:8082`) own the rest (`nvidia-smi`, 2026-07-06). A
~52 GB DiffusionGemma load would OOM or evict those live services. Freeing the GPU means
stopping `:8081` via `llauncher` — **operator-coordination infra, not something I touch
unilaterally, and you're asleep.** Surfaced as data, not routed around.

**To close it (pick one):**
- **(A)** Free the host GPU — `llauncher` stop `:8081`, then `cd <repo> && .venv/bin/python -m
  pytest -m live`, then restart `:8081`. A few minutes of chat-model downtime.
- **(B)** Run `pytest -m live` on the RTX-3090 (192.168.137.1) — it has the room and already
  ran DiffusionGemma live (#16). The tests are portable pytest.

## Gate 2 — the publish (awaiting your go)

**Mechanism (verified from `publish_action.yml`, not from memory):** the action triggers on
`push` to `main` touching `pyproject.toml` — **not** specifically a version bump. Merging
`p3 → main` changes pyproject → fires the action → `Comfy-Org/publish-node-action` publishes
whatever version is in pyproject (`0.1.0`) to registry.comfy.org.

**Unverifiable-from-here risk:** `REGISTRY_ACCESS_TOKEN` is a repo secret I cannot see. If it's
unset/expired, the merge lands publicly on `main` and the CI publish **fails silently** —
main bumped, nothing published. **Confirm the secret is set before merging.**

**Do not merge until:** live falsification passes (Gate 1) **and** the token is confirmed.
When both hold: git lane merges `p3 → main`, which also closes #18 (referenced by `dfb0769`).

## Questions for you (saved for the end, per your ask)

1. **Live run:** free the host GPU (A) or run on the 3090 (B)?
2. **`REGISTRY_ACCESS_TOKEN`** — confirmed set in repo secrets? (else silent publish fail)
3. **Publish go** once 1 + 2 are green — merge `p3 → main` = public 0.1.0?
4. **Close #16** (3090 passed live) and **#18** (fixed on `p3`, auto-closes on the merge)?
   #16 is `user:gate` so I left it for you; its pass evidence is commented on the issue.

## Notable finding — the pic was wrong, and that's the point

The RTX-3090 run's reasoning (sequence `0,1,2,6,16,44,120` → "284, 688, 1660") is **coherent,
confident, non-degenerate, and mathematically wrong.** The fitting rule is
`a(n)=2(a(n-1)+a(n-2))` → next is **328, 896, 2448**; the model used `2·a(n-1)+a(n-2)`, which
doesn't even reproduce the given `120` (gives 104). The leading `0` propped up its only clean
check (`2×1+0=2`). This is a live specimen of `loose-ends.md`'s "coherent-but-wrong basin"
(local self-consistency ≠ correctness) — and the case *for* the instrumentation: you can't
eyeball your way to catching it; you watch the commit-front. **Open research thread:**
premature-crystallization control, whose lever is the sigma→T schedule-shape transfer (borrow
Karras/exponential curves for the temperature anneal; a hotter-longer curve delays wrong-basin
commit). Natural next artifact: a `SIGMAS→heat` design doc/ADR — left for you to co-shape (it's
your research direction), not authored autonomously.

## Working ground

Clean after this pass (everything committed + pushed to `origin/p3-instrumentation`). Fast
suite green: **118 passed, 4 deselected**. Live suite: **4 collected, 0 run** (Gate 1).
