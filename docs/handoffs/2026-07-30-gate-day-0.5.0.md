# Handoff: 2026-07-30 — gate day: #163 stood up, run #145 batch closed, 0.5.0 blocked on #173 rework

**Authoritative ledger: issue #145** — every event below has a timestamped comment there. This handoff is the session-level map, not the record.

## What this session established

- **#163 (operator directive): seat-run fresh-install + live smoke gate before any release.** First execution ran today: install leg PASS, live leg FAIL — caught #173 (bf16 default path OOMs: dd2767c removed device_map="auto"; every <53GB card affected) and #174 (stale shipped examples). The gate paid for itself on first run.
- **KV-cache path fix-complete at fixture tier:** #162 (composite-config AttributeError, operator field report) + #166 (denoise↔sampler connection parity, shared emission helper, parity test) merged to main as PR #168 @ 7525300.
- **bf16 fit mechanism solved + banked:** docs/experiments/bf16-fit-mechanism/ (PR #164, merged): lazy-load — 42.4GiB GPU + 10.25GiB mmap-offloaded meta-device spill; 2.2 s/step live. Its "Trap #2" was cited by today's Opus gate to FAIL PR #177's first attempt.
- **Run #145 batch closed:** #170 (loader interrupt, merged), #158 (uv resolver, live-verified, merged), #171/#139 (merged), #172/#161 (merged), #124/#126/#120 (closed as already-done), #119 (left open — real remaining work, needs hand-merge of PR #121 vs PR #159 guard systems).
- **Branch bifurcation found and healed:** integration line is `pre-0.5.0-release`, main had forked; reconciled by merge @ 7cc67df → union suite 939/0 (later 966/0 after PR #176). #169's F0 failures confirmed as bifurcation shadow.
- **Host identity grounded:** inference-host (RTX-8000 48GB sm_75) vs shane-pc (RTX-3090 24GB sm_86, Windows/StabilityMatrix, = 192.168.137.1 = 192.168.1.2). Results now carry host tags (#101 floor amendment). Environment amendment awaiting ratification: operating-doctrine#40.
- **INT4/GGUF strategy:** Intel AutoRound INT4 is 5090-sized (deliberate, #131 comments); GGUF is the community-audience path — llama.cpp support exists as two working PR branches (llama.cpp#24423/#24427, research banked on #131/#15); ~59k GGUF downloads = the audience. INT4-on-Turing probe NEVER RUN (open question, H0 = kernel wall).

## In flight at handoff (the resume point)

1. **PR #177 rework (issue #173, RELEASE-BLOCKING):** first attempt FAILED Opus gate — restored spill path trips `_assert_no_meta_tensors` (meta-offload is legitimate under device_map="auto"; the assert predates coexistence). Rework in flight on branch `fix/173-restore-device-map`: spill-aware assert (exempt meta-because-offloaded via hf_device_map cpu/disk entries; stranded-meta still raises) + a fixture modeling the real spill regime. **Resume: check PR #177 for the rework push → fresh Opus gate → merge → smoke re-run.**
2. **#163 smoke re-run:** rig standing at /tmp/smoke-050 (6.3GB, evidence bundle; survives until host reboot). Re-run = pull fixed pack into the rig's custom_nodes checkout, relaunch, four scenarios (S-A gen, S-B kv-cache = #162 live proof, S-C log-writer = #166 live proof, S-D trace). PASS opens mechanics.
3. **0.5.0 mechanics (after smoke PASS):** CHANGELOG, version bump, PR #135 refresh/disposition (may be superseded — its headline landed at 193edd3), tag, registry publish + re-check 0.4.1's still-`Pending` registry activation. Stash@{0} drops after release. #169 closes when the release line lands on main.

## Operator decisions pending (none blocking the machine)

- #175: minimal node tooltips IN 0.5.0, or after? (operator requirement, scope call open)
- #167: run-record bundle type — operator wants a week to think (user:gate)
- OD#40: Environment host-identity amendment ratification
- #147: stays open for operator field-test on shane-pc
- #131/#15: GGUF promotion from pri:later — post-0.5.0 triage call

## Post-0.5.0 queue

#119 hand-merge lane · .ui.json examples residual (#174 comment) · #114 hygiene (incl. two stale worktrees for merged PRs #170/#171) · INT4-on-Turing probe · #175 full glossary pass · HT#226 (bell liveness line) implementation · HT#225 datapoints
