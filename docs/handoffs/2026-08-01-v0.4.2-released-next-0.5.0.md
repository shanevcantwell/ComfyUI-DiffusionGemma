# Handoff — 2026-08-01: v0.4.2 released (both channels); next bracket = 0.5.0 refactor

**State**: `main` = `a9e8bb9` (`release: 0.4.2`), tag `v0.4.2` == tip, registry publish run 30733058889 **success**. Suite 1008 passed / 0 failed (mocked tier) at `b654a31`; live field verification on the Windows/RTX-3090 box (sampler + denoiser clean; #207's raise fired live as designed). 0.4.2 milestone 12/12 closed; run ledger #203 closed.

## What closed this session (pointers, not prose)
- **Gate-first release per #196 — first application succeeded** and caught a real fakes-green regression before any version literal moved: #212 (PR #206's live-view mint declared a hidden input neither sampler nor denoiser accepted; two green Opus gates missed it because declared↔signature parity had no enforcement surface). Fix PR #213 + permanent executor-faithful parity test `tests/test_node_signature_parity.py`. Precedent recorded on #196 (decision text still to settle) and #163 (gate-formalization home).
- **Operator rulings executed end-to-end:** #207 kv-door fail-loud (live-verified raise, correct message); #175 minimal tooltips shipped (phase 2 remains, out of milestone).
- **Branch cull (#114):** ~62 refs + 1 stash reaped after per-ref content verification. Sole survivor: `fix/119-tied-weights-device-map-guard` @ `07eb764` — deliberate salvage for #119.
- **Field evidence banked:** bf16 split-load works on the 3090 (~5 s/it vs ~35 s+/it prior; #4, #16); loader placement-readback requirement (#160); gptqmodel scoped, Ampere-only (#211) + quant-guard backend-awareness bug (#210).

## Next bracket: 0.5.0 refactor (milestone: 4 open)
- **#129 — decompose `dgemma/loop.py`** (ADR-CDG-018, accepted). The entry task. The three sub-decisions (facade lifetime, excision.py cluster membership, Phase-0 oracle scope) are briefed on the issue.
- **#138 — topology remediation:** primitives layer + directory morph (ADR-CDG-018).
- **#140 — interrupt points** (slipped from 0.4.2; rides the loop decomposition — same surface).
- **#57 — consolidate the four dual-context `__package__` import gates.**
- Related, not in milestone: **ADR-CDG-019** (MCP-as-contract topology remediation) sits **proposed** — needs design-gate ratification before its work opens (#137/#134 adjacent).

## Not 0.5.0-fenced — live pri:now items a session could take first
- **#119** tied-weights corruption under split `device_map` (salvage branch retained; now field-relevant — the 3090 box runs split loads daily; its encode path is unverified).
- **#36** node cache not invalidated on `entropy_bound` change (sweeps serve stale results).
- **#35** architecture-review remainder (R1–R6 ledger).

## Parked operator decisions (no action till ruled)
- #193 README rewrite (user:gate, retained diff)
- #196 decision text (ordering proven once; settle it)
- Deeper live-evidence bracket (#59 E2E battery; the 3090 bf16 path is a proven substrate with zero llauncher tenancy conflict)
- `publish_action.yml` header still claims the registry token is "deliberately absent" — stale since 2026-07-14; one-line cleanup

## Standing constraints (unchanged)
- RTX-8000 tenancy: INT4 whole-fit and the llauncher chat model cannot coexist; schedule swaps explicitly.
- `plan.md` is tombstoned; direction = `ROADMAP.md`; evidence = `ARCHITECTURE.md`; tracking = the dashboard.
- Gate/author share one GitHub identity: formal PR approval state is unavailable; Opus PASS verdicts live as PR comments (twice observed; bank to harness-tools if it becomes load-bearing).

**Resumability:** this file + the issue dashboard + `ROADMAP.md` + ADR-CDG-018/019 are the full state; no session window is load-bearing.
