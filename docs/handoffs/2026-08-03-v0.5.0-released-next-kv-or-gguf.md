# Handoff 2026-08-03 — v0.5.0 released; next: KV-cache encoder and/or GGUF binary packaging

**Session summary.** 0.4.2 verified end-to-end; 0.5.0 scoped→ratified→executed→tagged→released in one day; #119 landed live-verified; the e2e battery went live-green for the first time; release/doc/ref surfaces fully reconciled. Operator set the next direction (below).

**Shipped: v0.5.0** (tag @ `c4beb3e`, GitHub Release created, marked Latest). Refactor-only by operator scoping: `dgemma/loop.py` decomposed into `config`/`compat`/`capture`/`excision` behind a re-export facade (ADR-CDG-018, #129 closed). Proof chain: golden-trace byte-identity oracle + AST-identical moved symbols + full suite 947/0 + live tier 4/4 on real bf16 weights. **Post-tag on main: #119** offload-aware tied-weights guard (`f04688f`, live-verified against a real 53 GB split placement; closes the field bug). **Registry still serves 0.4.2** — a 0.5.0 registry publish is an open operator call.

**E2E battery live-green (first time), #59:** S1/S2/S4 PASSED (run twice, identical), S3 strict-XFAIL riding open #9. Evidence comment on #59. Pack-identity gate now passes by direct call.

**Ground repairs, named individually (not bulk):**
- The deployed tree `/srv/dev/ComfyUI/custom_nodes/ComfyUI-DiffusionGemma` **was stranded on local branch `pre-0.5.0-release`** (a ref surviving the July cull) — switched to `main` and synced. **Pre-registered observable: the standing ComfyUI instance (sudo→`claude`, `--listen 0.0.0.0`) booted with old code and serves it until restarted** — any pack oddity observed there before a restart is old-code behavior, not a main bug. Restart is the operator's.
- GitHub Releases gap closed: 0.4.2 was the **first version ever** to skip its Release object (v0.2.0–v0.4.1 all had one) — v0.4.2 + v0.5.0 created retroactively; process datum banked on #196 (Release object as an explicit release step; CI-check enforcement candidate).
- Ref surface reconciled: origin = `main` + `release/0.4.1` only; all spent branches per-ref content-verified then pruned; worktrees clean.
- Doc tier reconciled at `17efc03` (README's stale "no quantized path" claims fixed, ADR tables/counts, path:symbol citations). CLAUDE.md staleness audit banked on #201 (file untouched, user:gate).

**Layering audit (2026-08-03, main @ 17efc03):** all eight rules' enforcement surfaces pass fresh. Two findings banked: **#220** (ARCHITECTURE.md drift — rule-1 contract restatement post-ratified-repoint, rule-7 `should_cancel` sanctioning, file lists, stale battery gap — folds into the #138 bracket's Phase-6 doc pass) and **#221** (`logit_hook` engine-internal by convention only; ingress doesn't reject a bare surface closure — rule-7 hole, small fix + test).

**llama.cpp thread:** rung 0 green (pin candidate `c3fb972`: CPU 1m27s, CUDA sm_75 9m46s). Full analysis banked on #131 (2026-08-03 comment): three-way conformance matrix (core semantics identical across CDG/#24423/#24427; divergence axis = self-cond signal fidelity, falsifiable H0), seam map (DiffusionFrame-as-wire-format is the abandonability seam), rung-1 measurement plan (GPU-window-gated), upstream value channels. All decisions above rungs 0–1 remain operator-gated.

**Next bracket — operator-directed 2026-08-03, ordering decided at opening:**
1. **KV-cache encoder completion** — make the encode→denoise path actually consume the encoded cache. Lineage: ADR-CDG-012 (socket + nodes exist), #47 (seam injection), #62 (impl plan), ingress currently fail-loud inert by design (#207/#209). Done looks like: a denoise run with injected `kv_cache` produces output consistent with the equivalent aio run, prefill skipped, live-verified.
2. **Bespoke llama.cpp binaries in the release package** — rung 4 of the #131 ladder: CI-built `llama-diffusion-cli` from the owned pin (`c3fb972` build-proven on this host), checksummed release artifacts, owned provenance (third-party prebuilts remain declined). Design questions at the bracket: platform matrix, release-asset vs registry packaging, maintenance commitment.
Queued behind: topology bracket (#138 — ADR-CDG-019 accepted, `dgemma_mcp/` rename decided, #220 rides it; #57, #140) and #219 (salience decomposition + prose evacuation).

**Open ProdM items:** #210 acceptance criteria; #151 mcp-2.x surface ruling; #193 README rewrite gate; #201 CLAUDE.md gate; 0.5.0 registry-publish call; ComfyUI instance restart; S3 thinking-xfail rides #9.

**Session-discipline banked (harness-tools):** #242 (+wrong-path-readback variant), #243 (orient forward-direction doc), #245 (PASS_WITH_FINDINGS vocabulary), #246 (mcp namespace-shadow class). Grounding-failure corrections ledger lives on #129's comment thread.
