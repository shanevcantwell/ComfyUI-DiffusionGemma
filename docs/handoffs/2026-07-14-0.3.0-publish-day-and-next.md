# Handoff — 0.3.0 publish day (2026-07-14) and the queue behind it

Written at session close 2026-07-14 ~04:30 UTC (2026-07-13 evening US/Mountain).
Ground state: main @ `4b47f75` = tag `v0.3.0` (annotated, release notes on GitHub);
publish workflow is dispatch-only by design (PR #77); registry still holds 0.2.0;
20 downloads on the planted flag. Test state at RC: 444/444 green, 99% coverage.
Task ledger: task #1 = the publish runbook. Every item below is dispatchable cold —
context lives in the cited artifacts, not in any session window.

## A — Operator-led, tomorrow daytime (1-week flag anniversary)

1. **Windows morning testing.** Pull `v0.3.0` (`git fetch --tags && git checkout v0.3.0`
   or fresh `git clone --branch v0.3.0 …`), install into ComfyUI, run the example
   workflows live. *Doubles as the verification readback for closed issue #25*
   (Manager reads requirements.txt): if install resolves deps clean, apply `verified`
   to #25.
2. **Workflow aesthetics** (`examples/*.json`) — operator's pass, operator's taste.
3. **(Optional) release-notes voice pass** on the GitHub v0.3.0 release text —
   changelog is honest but agent-registered; the 20 downloaders deserve the
   operator's register.
4. **Publish** (task #1 runbook): Settings → Secrets → Actions → new repo secret
   `REGISTRY_ACCESS_TOKEN` → Actions → "Publish to Comfy registry" → Run workflow →
   verify green run + registry listing shows 0.3.0. Version already outruns the
   registry (0.3.0 > 0.2.0) so it publishes clean. No merge can fire this workflow;
   only the button can.

## B — Agent-dispatchable in clean context (order matters, gates as usual)

5. **Coverage residual PR** — one branch, one gate: #74 (one unit test,
   `_wrap_text` blank-paragraph) + #75 (three `# pragma: no cover` + reason
   comments). Both `auto:fix, pri:next`, file-disjoint from in-flight PRs.
   Outcome: measured 100%, exclusions self-documenting. Then optionally add
   `--cov-fail-under=100` wherever coverage is next wired.
6. **#76 draft** (`auto:draft, pri:later`, after 5 merges): recommend retire-vs-refresh
   of `test-coverage-plan.md`; session-close recommendation on record is *retire*
   (decisions/README.md precedent), but that is the draft's case to make and the
   operator's to ratify.
7. **PR #71 Opus gate → merge** (CDG-010/011 P3: pin participant + logit-mask hook,
   constraints live end-to-end). Highest-leverage open item; unlocks 0.4.0's core
   and the research thread (D). *Operator eyes on the node-facing constraint UX
   before/at gate — input naming and pin ergonomics are taste, and about to be API.*
8. **PR #70 Opus gate → merge** (battery P2, scenarios S2–S4 incl. the #9-catcher).
   Merge-confidence infra; live phases still operator-gated per #59 §5. The
   cancellation scenario (#38's test commitment) is banked as a #59 comment —
   add it to the scenario list when battery work next opens.
9. **CI test workflow** — `user:gate`: propose `.github/workflows/test.yml` (unit
   suite on PR/main push; marker gating already excludes live/e2e; torch CPU wheel
   cached). Rationale on record (2026-07-13 session): every green signal currently
   comes from agents running pytest locally on trust; CI gives GitHub-native
   red/green independent of the asserting agent, defense-in-depth under
   auto-merge-on-gate-PASS. Draft as PR for operator review — workflow/infra
   changes are not applied unilaterally.
10. **0.4.0 scoping revisit** after #71 lands — the agreed split: 0.3.0 = "see the
    anneal" shipped as-is; 0.4.0 = "steer and read" (constraints via #71, #64 P4
    walker as the swing item, #61 P-B/P-C DISTRIBUTION capture, β cloze-renoise R1,
    #72 JSONL run-log). Re-scope against runway then.

## C — Verified-tag backlog (light, any session)

Eleven closed issues lack `verified` (sweep 2026-07-13): #55 #52 #30 #26 #25 #22
#21 #20 #19 #18 #1. #25 resolves via A-1. Fold the rest into the next autonomous
run's close ritual rather than a dedicated chore.

## D — Research thread (gated on #71/0.4.0 capability; operator + GPU)

Preregistered, stamped in `design-docs/incoming-ideas/DG-runs/RESOURCES.md`
(committed `f6c4b39`):
- **Pinned-givens reversal** — DG's untested 2×2 cell ([H-STAGING]): pin the source
  content words, re-run the reversal prompt; prediction = template fills with
  conserved words.
- **β-conductivity** ([H-BETA-CONDUCTIVITY]): β<1 renoise on the reversed-code
  prompt reduces within-word scramble at fixed step budget. Scoreable with
  `DG-runs/tools/dg_run_report.py compare`.
- **Twin-axes measurement** ([GAP-TWIN-AXES]): tuned-lens depth-of-resolution on
  gemma-4-26B vs commit-step on DG, matched prompts — *check [UNVERIFIED-LINEAGE]
  first* (whether DG weights derive from AR Gemma; model card).
- Standing hazard note: raw high-entropy/reversed text in model-visible context
  trips harness safety filters (observed kill 2026-07-13). Move it by file pointer;
  the tools README documents the discipline; dg_run_report clamps by code.

## Loose ends deliberately left

- llauncher: #292 (Streamlit config writes broken registry-wide since ~07-04 +
  draft-model fields invisible; crash evidence in comments). Operator's repo,
  operator's schedule. Config backup `~/.llauncher/config.json.bak-2026-07-13-spec-strip`
  kept until #292 resolves (the live config now has NO spec-draft args for
  gemma-4-26B, per operator directive: no assistant, no MTP).
- design-docs ` M agent-constitution/README.md`: pre-existing pointer-repoint
  (harness-tools → operating-doctrine, matches ADR-CON-0009 canon); shown to
  operator at session close, disposition theirs.
- CDG-012 KV_CACHE phases 2–5, #61 P-B/P-C, #64 P4: designed, unstarted, in ADRs.

## Addendum — 2026-07-15, numeral-counts finding banked

1. **Numeral-counting overturn banked.** New experiment record
   `design-docs/experiments/2026-07-15-dg-numeral-counts-update-in-response/README.md`:
   a clean single-token numeral tally shows the diffusion visibly updating counts against
   the annealing canvas (count of '4': 2→3 slaved to the 10th canvas position resolving) —
   direct counterexample to "counts freeze one-shot, never reconciled against evidence."
2. **Gatsby conclusion superseded, not deleted.** The 2026-07-14 gatsby record
   (`design-docs/experiments/2026-07-14-dg-gatsby-counts-ar-prior-latch/README.md`) now
   carries a superseding note at its top: its *generalization* ("diffusion performs no
   checkable work") is overturned; its raw observations stand, reattributed to a
   multi-token-word confound rather than an absence of reconciliation.
3. **Open next step:** the sudoku/[0-9]-abundance mechanism hypothesis (operator
   free-association, recorded in the new experiment record) — "[0-9] cheap to produce from
   KVs, abundant in a KV set containing all necessary numerals" — is untested. Test path:
   the KV-separation seam (#47, ADR-CDG-012) — inject a deliberately small/closed
   vocabulary KV set vs. an over-provisioned one and compare reconciliation cleanliness.
   Also banked as a comment on #28 (Sudoku-class flagship).
